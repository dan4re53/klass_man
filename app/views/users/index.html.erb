<% provide(:title, 'All users') %>
<h1>All users</h1>

<% if current_user.admin?%>
<%= button_to "Add New Student", addstudent_path, method: :get %>
<% end %>
<%= will_paginate %>

<ul class="users">
	<%= render @users %>
</ul>

<%= will_paginate %>